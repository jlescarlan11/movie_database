<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <title>Reset Password - Movie Database</title>
  </head>
  <body>
    <main
      class="container max-w-7xl mx-auto flex flex-col items-center gap-8 p-8"
    >
      <div class="flex flex-col items-center justify-center gap-4 mb-8">
        <h1 class="text-3xl sm:text-5xl font-bold">Movie Database</h1>
      </div>

      <div class="flex flex-col items-center sm:justify-center">
        <form
          action="/password-reset"
          method="POST"
          class="border border-secondary max-w-fit p-4 sm:p-8 rounded-lg"
        >
          <div class="text-center mb-4">
            <h2 class="text-xl sm:text-3xl font-bold">Forgot Password?</h2>
            <p class="text-text/80">
              Enter your email address and we'll send you a link to reset your
              password.
            </p>
          </div>
          <%- include("partials/errors") %>
          <div class="mb-4">
            <div class="flex flex-col flex-1 group mb-2">
              <label
                for="email"
                class="text-xs font-medium text-text/80 group-focus-within:text-text mb-2"
                >Email address</label
              >
              <input
                type="email"
                id="email"
                name="email"
                placeholder="Enter your registered email"
                class="border border-secondary rounded-lg p-2 text-xs"
                required
                autofocus
              />
            </div>
          </div>

          <div class="w-full flex">
            <input
              type="submit"
              value="Find Email"
              class="mx-auto sm:flex-1 bg-accent hover:bg-accent/80 transition-all ease-in-out duration-200 p-2 rounded-lg font-medium"
            />
          </div>

          <p class="text-xs text-center">
            Remember your password?
            <a
              href="/"
              title="Go to Login Form"
              class="hover:text-accent/80 text-accent font-medium"
              >Log In now</a
            >
          </p>
        </form>
      </div>
    </main>
  </body>
</html>
